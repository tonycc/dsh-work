# dsh-work 前端原型评审说明

**原型版本：** V0.2
**更新日期：** 2026-08-29
**评审状态：** 已确认，作为 MVP 实施交互基线
**实现位置：** `apps/workbench-web`、`apps/admin-web`、`packages/*` 与 `server`
**数据来源：** Node.js 原型接口 + 进程内仓储；不连接真实企业系统、模型、PostgreSQL 或 DSH

## 一、启动方式

```bash
pnpm install
pnpm dev:all
```

员工工作台默认地址为 `http://localhost:4174`，管理后台默认地址为 `http://localhost:4180`，Node.js 服务端默认地址为 `http://127.0.0.1:4190`。两个前端端口均采用严格模式，并分别把 `/api` 代理到服务端。

也可以分别启动：

```bash
pnpm dev:workbench
pnpm dev:admin
pnpm dev:server
```

质量检查：

```bash
pnpm typecheck
pnpm validate:ui
pnpm lint
pnpm build
```

## 二、原型结构

原型已经按最终依赖方向维护：员工工作台与管理后台是两个独立 Vue 3 应用，各自拥有应用壳层、路由、Pinia、接口客户端、数据传输对象、构建产物和发布入口；两者只通过工作区软件包复用无业务状态的设计变量与展示组件，不互相引用应用源码，也不共享状态仓库、用户状态或服务端领域类型。

两个应用通过 `packages/design-tokens` 共用同一套字体等级，保证正文、辅助文字、标题和指标字号一致；管理后台在共享基础设计变量之上独立加载 `apps/admin-web/src/styles.css`，员工工作台不会继承管理端的页面密度、壳层和 Element Plus 覆盖规则。

目录边界：

```text
apps/
├── workbench-web          # 员工工作台
└── admin-web              # 管理后台

packages/
├── design-tokens          # 颜色、字号、间距等共享设计变量
├── ui-core                # 无业务状态的基础展示组件
├── workbench-components   # 员工端业务组件
└── admin-components       # 管理端无状态壳层与展示组件

server/
└── src
    ├── http               # 员工端接口 / 管理端接口两个门面
    ├── modules            # 同一模块化单体中的应用与领域边界
    └── infrastructure     # 原型仓储等可替换适配器
```

技术结构：

- Vue 3 + TypeScript + Vite；
- 两个应用独立维护 Vue Router、Pinia、接口客户端和数据传输对象；
- Element Plus 管理端组件；
- Node.js + TypeScript 模块化单体，提供 `/api/workbench/v1` 与 `/api/admin/v1` 两个接口门面；
- `server/src/infrastructure/prototype` 提供可追溯业务数据和内存仓储；
- 员工端对话运行动画仍是前端交互模拟，服务端健康接口会明确标记 DSH 未接入；
- 原型仓储后续可替换为 PostgreSQL 仓储，而两个前端接口边界保持不变。

## 三、评审入口

| 应用 | 页面 | 路由 | 重点确认内容 |
|---|---|---|---|
| 员工工作台 | 工作台 | `/workbench` | 沉浸式新对话入口、工作模式、能力快捷栏、引用/文件入口、工作空间与权限上下文 |
| 员工工作台 | 对话页 | `/conversations/:id` | 中心消息流、固定底部输入、连续追问、审批、成果和对话详情抽屉 |
| 员工工作台 | 工作空间 | `/workspaces` | 团队工作空间列表、责任团队、成员、权限提示和共享文件 |
| 员工工作台 | 工作空间详情 | `/workspaces/:id` | 顶部“对话 / 共享文件 / 成果”页签、锁定当前团队空间、右侧空间上下文和移动端信息抽屉 |
| 员工工作台 | 我的成果 | `/artifacts` | 成果列表、版本、来源运行、预览和下载 |
| 员工工作台 | 用户中心 | `/settings` | 从左下角用户入口查看企业身份、数据范围、输出与通知偏好 |
| 管理后台 | 运营概览 | `/overview` | 运行、成功率、模型用量、异常和最近审计 |
| 管理后台 | Agent 管理 | `/agents` | Agent 启停、当前版本、版本历史、版本回滚和发布记录 |
| 管理后台 | Skill 与工具 | `/capabilities` | Skill 管理、预置工具目录与权限、预置连接器状态和健康检查；一期不开放自定义工具注册或连接器接入 |
| 管理后台 | Runtimes | `/runtimes` | Runtime 健康、容量、活动 Worker、最大并发、执行超时、接收任务/排空/停用和健康检查 |
| 管理后台 | Session 列表 | `/sessions` | Session、用户、工作空间、Agent、Runtime、Token、Run 和 Trace 治理元数据 |
| 管理后台 | 工作空间 | `/workspaces` | 团队工作空间、责任团队、负责人、成员、Session、文件、成果和可用 Agent |
| 管理后台 | 模型用量 | `/model-usage` | 模型调用量、Token、成本、时延、失败状态和单次调用下钻 |
| 管理后台 | 成员管理 | `/members` | 企业单点登录成员、角色、部门、数据范围及生效能力 |
| 管理后台 | 权限与数据范围 | `/permissions` | 角色权限、工具允许列表、业务数据范围和审批策略的可保存配置 |
| 管理后台 | 审计记录 | `/audit` | 运行、模型、工具、连接器、成果事件与脱敏详情 |
| 管理后台 | 系统健康 | `/health` | 模块化单体、DSH 执行进程和外部依赖状态 |

## 四、角色预览

员工工作台左下角可以切换四种原型角色；切换到平台管理员或安全审计员后可打开独立管理后台。管理后台通过自己的会话接口模拟企业单点登录返回的平台管理员身份，并可切换平台管理员与安全审计员视图。

- 普通员工：只显示员工工作区；
- 部门负责人：员工工作区和部门数据范围；
- 平台管理员：显示完整管理后台和写操作；
- 安全审计员：显示只读管理与审计页面，不显示发布、创建等写操作。

角色切换用于确认页面权限和导航表现，不代表真实鉴权实现。真实版本中两个前端共享企业单点登录与后端授权结果，但独立维护路由和组件；任何前端状态都不能替代后端鉴权。

## 五、关键原型场景

| 场景 | 原型入口 | 可验证交互 |
|---|---|---|
| 新对话完整链路 | 工作台输入并发送 | 创建对话和首轮运行、跳转对话页、生成来源和成果 |
| 空间内新对话 | 点击任一工作空间 | 新对话自动绑定当前空间；顶部页签切换对话、共享文件和成果，右侧查看成员与权限 |
| 引用空间文件 | “共享文件”页签点击“引用到对话” | 自动切回“对话”页签、进入文件分析模式并将指定文件带入输入框 |
| 空间成果预览 | “成果”页签点击“预览” | 在当前工作空间页面查看成果元数据与来源运行 |
| 连续追问 | 任一对话页底部输入 | 消息追加到当前对话，并启动新的运行尝试 |
| 成功对话 | `run-260828-001` | 最终回答、来源、两个成果文件与执行详情 |
| 运行中对话 | `run-260828-002` | 实时回答状态与停止本轮执行 |
| 等待审批 | `run-260827-004` | 查看工具、数据范围和只读说明，确认或拒绝 |
| 失败对话 | `run-260826-008` | 连接器超时原因、处理建议和本轮运行尝试重试 |
| 知识问答 | `run-260827-002` | 制度答案、文档版本与来源依据 |
| Agent 生命周期 | 管理后台“Agent 管理” | 启用、停用、发布记录、版本历史及回滚；回滚后模型与能力配置同步到目标版本 |
| 成员与生效权限 | 管理后台“成员管理” | 按姓名、部门和状态筛选，查看单点登录状态、数据范围及生效 Agent / 工具 |
| 模型用量下钻 | 管理后台“模型用量” | 按模型、Agent 和状态筛选，查看单次 Token、成本、时延及关联审计链路 |
| 权限策略配置 | 管理后台“权限与数据范围” | 保存角色 Agent / 工具 / 数据范围，以及工具授权角色、数据范围和审批策略 |

原型中每轮提问会创建一次运行，并在约 5 秒内依次完成：

1. 理解问题与校验权限；
2. 准备文件或企业数据；
3. 执行业务分析；
4. 组织答案并生成成果。

## 六、已确认的产品决策

1. 员工首页只保留一个主要新对话输入焦点；
2. 左侧采用“发起对话 + 工作入口 + 最近对话 + 个人身份”的轻量结构；
3. 工作模式按“日常办公、企业查询、数据分析”组织；
4. 能力快捷栏覆盖文档、知识、订单、生产、库存、文件、经营和报告；
5. 新对话默认不加入工作空间，由用户主动选择团队工作空间；
6. 一期只支持团队工作空间，不建设个人工作空间；
7. 团队工作空间属于责任团队，不绑定创建人个人；
8. 工作空间详情使用“对话 / 共享文件 / 成果”顶部页签；
9. 空间内对话锁定当前工作空间，右侧仅展示责任团队、权限和成员等稳定上下文；
10. 添加内容按“本地文件、工作空间文件、企业数据”组织；
11. 自动、快速、深度三种执行模式在 MVP 中保留；
12. 最近对话承担快速回访，不提供独立的全量对话记录页面；
13. 活动卡只显示当前最需要关注的一个执行中或待审批对话；
14. 对话页采用“中心消息流 + 固定底部输入”，执行步骤、来源、运行和成果进入详情区；
15. 审批必须说明工具、数据范围和操作类型；
16. 成果提供版本、预览、下载和来源 Run 追溯；
17. Agent 创建采用“定义 Agent → 配置能力和权限 → 测试并创建”，创建页不显示负责人，欢迎语选填，MVP 不配置模型策略；
18. Skill 标识由系统自动生成，创建页不要求用户填写；
19. 一期不开放自定义 Tool 和 Connector，只管理实施团队预置能力；
20. Runtimes 位于“安全与运维”，一期允许配置最大并发 Worker、单次执行超时和调度状态；
21. 管理后台采用浅色、高信息密度的企业管理规范；员工工作台独立维护沉浸式交互规范。

## 七、原型暂不包含

- 真实企业单点登录、生产级服务端鉴权和路由鉴权；原型配置仅保存在进程内仓储，服务重启后恢复基线数据；
- 真实文件上传、病毒扫描、在线预览和对象存储；
- 真实服务端事件流、PostgreSQL 和 DSH 执行进程；
- 真实模型、工具、连接器和企业系统调用；
- PPT 或演示文稿生成；
- 可视化 Agent 编排器；
- 个人工作空间及用户自建个人空间；
- 独立的全量对话记录页面；
- 企业微信、飞书和桌面辅助端。

确认功能与交互后，优先在现有 Node.js 模块化单体中逐项实现命令接口、服务端鉴权、PostgreSQL 仓储、服务端事件流和运行时适配器；无需改变两个前端及双接口门面的总体边界。

## 八、管理端页面规范（已确认）

管理后台采用与 `wangjia-sh` 管理端一致的页面规范，并在 dsh-work 内维护独立实现，不直接依赖其他项目源码：

- `apps/admin-web/src/styles.css` 是管理端颜色、字号、间距、圆角、页面骨架和 Element Plus 覆盖规则的实现来源；
- `packages/admin-components/src/AdminShell.vue` 是浅色分组导航、顶部单一页面标题和用户菜单的壳层基准；
- 页面使用 `.ops-page`、`.content-panel`、`.filter-panel`、`.filter-bar`、`.metric-grid` 和 `.data-table` 等统一结构；
- 页面标题只由路由 `meta.title` 和管理壳层渲染一次；页面内容不重复出现 H1；
- 列表必须具备加载态、空态、错误提示和稳定的行操作；无权限写操作不渲染；
- 表单使用顶部标签，标准新建/编辑弹窗为 480–560px，复杂权限表单可扩展至 680–960px；
- 页面局部样式不得硬编码颜色、字号、圆角或间距，新增视觉语义先补充管理端设计变量；
- `apps/admin-web/scripts/validate-ui-contract.mjs` 校验路由标题、页面骨架、列表闭环、操作锚点和禁止项，交付前执行 `pnpm validate:ui`、`pnpm typecheck`、`pnpm lint` 与 `pnpm build`。
